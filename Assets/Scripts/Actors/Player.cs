using UnityEngine;

namespace Actors
{
    public class Player : MonoBehaviour
    {
        public bool Ping()
        {
            // Check if the player is still valid
            return true;
        }
    }
}